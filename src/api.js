export const fetchAPI = (date) => {
    // Mock API call, returns a list of times
    return ['17:00', '18:00', '19:00', '20:00', '21:00'];
  };
  
  export const submitAPI = (formData) => {
    // Mock API call, returns true for successful submission
    return true;
  };
  